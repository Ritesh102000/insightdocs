spring:
  application:
    name: insightdocs
  docker:
    compose:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/insightdocs
    username: ritesh
    password: password123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true